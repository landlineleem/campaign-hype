---
phase: 03-stats-and-sequence
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - report/overlay.js
  - report/index.html
  - assets/report.css
  - report/main.js
autonomous: true
requirements: [STAT-03, CELB-03]

must_haves:
  truths:
    - "After revealStats resolves, showBenchmarkPopup appears with the candidate's deliverability % and a contextual celebration message"
    - "The benchmark popup only shows the celebration message when deliverabilityPct exceeds 85 (the industry average floor); otherwise shows a neutral message"
    - "The full sequence fires in order: flyToDistrict -> runDotCascade -> revealStats -> showBenchmarkPopup — with no overlap and no raw setTimeout between stages"
    - "report/main.js Stage 3 placeholder is replaced with real await calls to overlay.revealStats and overlay.showBenchmarkPopup"
    - "The benchmark popup can be dismissed by clicking/tapping it"
  artifacts:
    - path: "report/overlay.js"
      provides: "showBenchmarkPopup(payload) — real implementation replacing the stub from Plan 01"
      exports: [revealStats, showBenchmarkPopup, celebrate]
    - path: "report/index.html"
      provides: "Benchmark popup DOM: #benchmark-popup with #benchmark-pct, #benchmark-message, #benchmark-dismiss"
      contains: "id=\"benchmark-popup\""
    - path: "assets/report.css"
      provides: "Benchmark popup styles: slide-in animation, gold accent, dismiss button"
      contains: "benchmark-popup"
    - path: "report/main.js"
      provides: "Wired sequence: await revealStats(payload) then await showBenchmarkPopup(payload)"
      contains: "overlay.revealStats"
  key_links:
    - from: "report/main.js async IIFE"
      to: "report/overlay.js revealStats()"
      via: "await overlay.revealStats(payload)"
      pattern: "await.*revealStats"
    - from: "report/main.js async IIFE"
      to: "report/overlay.js showBenchmarkPopup()"
      via: "await overlay.showBenchmarkPopup(payload)"
      pattern: "await.*showBenchmarkPopup"
    - from: "report/overlay.js showBenchmarkPopup()"
      to: "report/index.html #benchmark-popup"
      via: "getElementById and style.display = 'flex'"
      pattern: "getElementById.*benchmark"
    - from: "#benchmark-dismiss click"
      to: "showBenchmarkPopup Promise resolve()"
      via: "addEventListener click -> resolve()"
      pattern: "benchmark-dismiss"
---

<objective>
Implement the industry benchmark popup and wire the complete Phase 3 sequence into main.js — replacing the Stage 3 placeholder with real await calls.

Purpose: The benchmark popup is the emotional climax of Phase 3. "Your 96.2% CRUSHED the 88% industry average!" reframes raw statistics as a competitive win. Wiring the full sequence (map -> dots -> stats -> benchmark) completes the choreography requirement (CELB-03).
Output: showBenchmarkPopup() in overlay.js (real impl), benchmark popup HTML/CSS, main.js Stage 3 wiring.
</objective>

<execution_context>
@/home/vpliam/.claude/get-shit-done/workflows/execute-plan.md
@/home/vpliam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stats-and-sequence/03-01-SUMMARY.md

<interfaces>
<!-- report/overlay.js exports after Plan 01 (to be extended in this plan) -->
  export function revealStats(payload)        // Promise — resolves when all counters done
  export function showBenchmarkPopup(payload) // stub returning Promise.resolve() — REPLACE in Task 1
  export function celebrate()                 // stub returning Promise.resolve() — leave for Phase 4

<!-- payload.deliverabilityPct range and benchmark constants -->
  INDUSTRY_AVERAGE_FLOOR = 85    // below this = no celebration message
  INDUSTRY_AVERAGE_CEIL  = 92    // above floor, below ceil = "above average"
  Message logic:
    pct >= 92  -> "CRUSHED the industry average!"   (gold celebration)
    pct >= 85  -> "Beat the industry average!"      (positive)
    pct < 85   -> "Industry average: 85-92%"        (neutral, no judgment)

<!-- report/main.js Stage 3 placeholder to replace (lines 78-82 approximately) -->
  // Stage 3 placeholder (Phase 3: reveal stat counters + benchmark popup)
  // await overlay.revealStats(payload);
  // await overlay.showBenchmarkPopup(payload);
  console.log('[main.js] Phase 2 sequence complete - ready for Phase 3 stats');

  Replace with:
  import { revealStats, showBenchmarkPopup } from './overlay.js';
  // ... (inside the async IIFE after runDotCascade resolves)
  await revealStats(payload);
  await showBenchmarkPopup(payload);
  console.log('[main.js] Phase 3 sequence complete');

<!-- Architecture boundary (still applies) -->
  overlay.js NEVER imports from map.js
  Sequencer coordinates both — overlay.js only touches DOM elements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark popup HTML to report/index.html and CSS to assets/report.css</name>
  <files>report/index.html, assets/report.css</files>
  <read_first>
    Read report/index.html to see the current overlay layer stack and where #stats-panel was added in Plan 01. The benchmark popup goes after #stats-panel and before the script tag. Read assets/report.css tail (last 80 lines) to see what Plan 01 added — append new CSS after the stats panel rules. The popup should slide in from the bottom using a CSS keyframe animation. It starts hidden (display:none), is shown by showBenchmarkPopup(), and is dismissed by clicking the dismiss button or anywhere on the overlay background.
  </read_first>
  <action>
In report/index.html: Insert the benchmark popup block immediately after the closing div of #stats-panel (before the script tag):

  &lt;!-- Layer 4c: Industry benchmark popup — shown after revealStats resolves --&gt;
  &lt;div id="benchmark-popup" class="overlay-layer benchmark-overlay" style="display:none"&gt;
    &lt;div class="benchmark-card" id="benchmark-card"&gt;
      &lt;div class="benchmark-pct" id="benchmark-pct"&gt;&lt;/div&gt;
      &lt;div class="benchmark-message" id="benchmark-message"&gt;&lt;/div&gt;
      &lt;div class="benchmark-comparison"&gt;Industry average: &lt;span class="benchmark-avg"&gt;85–92%&lt;/span&gt;&lt;/div&gt;
      &lt;button class="benchmark-dismiss" id="benchmark-dismiss" type="button"&gt;See Full Results&lt;/button&gt;
    &lt;/div&gt;
  &lt;/div&gt;

Use real angle brackets in the file (not entities).

In assets/report.css: Append these rules to the END of the file after the stats panel rules from Plan 01:

/* ── Benchmark Popup ── */
@keyframes benchmark-slide-in {
  from { transform: translateY(40px); opacity: 0; }
  to   { transform: translateY(0);    opacity: 1; }
}

.benchmark-overlay {
  z-index: 25;
  background: rgba(0, 0, 0, 0.55);
  backdrop-filter: blur(2px);
  pointer-events: all;
  cursor: pointer;
}

.benchmark-card {
  background: rgba(10, 10, 10, 0.92);
  border: 1px solid rgba(255, 184, 0, 0.4);
  border-radius: 14px;
  padding: 28px 32px 24px;
  text-align: center;
  max-width: 340px;
  width: calc(100% - 32px);
  animation: benchmark-slide-in 0.5s cubic-bezier(0.22, 1, 0.36, 1) forwards;
  pointer-events: all;
  cursor: default;
}

.benchmark-pct {
  font-size: clamp(2.4rem, 8vw, 3.6rem);
  font-weight: 700;
  color: #FFB800;
  line-height: 1;
  margin-bottom: 8px;
  font-family: 'Georgia', serif;
}

.benchmark-message {
  font-size: clamp(1rem, 3vw, 1.25rem);
  color: #ffffff;
  font-weight: 600;
  margin-bottom: 10px;
  line-height: 1.3;
}

.benchmark-comparison {
  font-size: 0.75rem;
  color: rgba(255, 255, 255, 0.45);
  letter-spacing: 0.05em;
  margin-bottom: 20px;
}

.benchmark-avg {
  color: rgba(255, 184, 0, 0.6);
  font-weight: 600;
}

.benchmark-dismiss {
  background: #FFB800;
  color: #0a0a0a;
  border: none;
  border-radius: 8px;
  padding: 11px 28px;
  font-size: 0.9rem;
  font-weight: 700;
  cursor: pointer;
  letter-spacing: 0.04em;
  transition: background 0.15s ease;
  pointer-events: all;
}

.benchmark-dismiss:hover,
.benchmark-dismiss:focus {
  background: #ffd04f;
  outline: 2px solid #FFB800;
  outline-offset: 2px;
}

@media (max-width: 480px) {
  .benchmark-card {
    padding: 22px 20px 20px;
  }
}
  </action>
  <verify>
    <automated>grep -c 'id="benchmark-popup"' /home/vpliam/campaign-hype/report/index.html && grep -c 'id="benchmark-dismiss"' /home/vpliam/campaign-hype/report/index.html && grep -c 'benchmark-slide-in' /home/vpliam/campaign-hype/assets/report.css && cd /home/vpliam/campaign-hype && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="benchmark-popup"' report/index.html returns 1 match
    - grep 'id="benchmark-pct"' report/index.html returns 1 match
    - grep 'id="benchmark-message"' report/index.html returns 1 match
    - grep 'id="benchmark-dismiss"' report/index.html returns 1 match
    - grep 'display:none' report/index.html includes the benchmark-popup line (starts hidden)
    - grep '@keyframes benchmark-slide-in' assets/report.css returns 1 match
    - grep 'benchmark-overlay' assets/report.css returns a match
    - npm run build exits 0
  </acceptance_criteria>
  <done>Benchmark popup HTML and CSS exist. Popup starts hidden, has pct/message/dismiss elements, slide-in animation defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement showBenchmarkPopup in overlay.js and wire full sequence into main.js</name>
  <files>report/overlay.js, report/main.js</files>
  <read_first>
    Read report/overlay.js in full (created in Plan 01) — find the showBenchmarkPopup stub and replace it. Read report/main.js in full — find the Stage 3 placeholder comment block (lines ~78-82) and replace it with real await calls. Also find the import block at top of main.js and add overlay.js import.
  </read_first>
  <action>
TWO changes required:

--- CHANGE 1: report/overlay.js ---

Replace the showBenchmarkPopup stub with a real implementation. The existing stub is:
  export function showBenchmarkPopup(payload) { /* TODO Phase 3 Plan 02 */ return Promise.resolve(); }

Replace with this implementation:

const INDUSTRY_FLOOR = 85;
const INDUSTRY_CEIL  = 92;

export function showBenchmarkPopup(payload) {
  return new Promise((resolve) => {
    const popup  = document.getElementById('benchmark-popup');
    const pctEl  = document.getElementById('benchmark-pct');
    const msgEl  = document.getElementById('benchmark-message');
    const dismiss = document.getElementById('benchmark-dismiss');

    if (!popup || !pctEl || !msgEl) {
      console.warn('[overlay.js] benchmark popup elements not found — skipping');
      resolve();
      return;
    }

    // Populate content — textContent only, never innerHTML
    pctEl.textContent = payload.deliverabilityPct.toFixed(1) + '%';

    if (payload.deliverabilityPct >= INDUSTRY_CEIL) {
      msgEl.textContent = 'CRUSHED the industry average!';
    } else if (payload.deliverabilityPct >= INDUSTRY_FLOOR) {
      msgEl.textContent = 'Beat the industry average!';
    } else {
      msgEl.textContent = 'Campaign delivery results';
    }

    popup.style.display = 'flex';

    // Dismiss: clicking the dismiss button OR anywhere on the dark overlay background
    function onDismiss() {
      popup.style.display = 'none';
      popup.removeEventListener('click', onDismiss);
      if (dismiss) dismiss.removeEventListener('click', onDismiss);
      resolve();
    }

    // Clicking the overlay background (not just the card) also dismisses
    popup.addEventListener('click', onDismiss);
    // Prevent card clicks from bubbling to overlay (so card interior doesn't dismiss)
    const card = document.getElementById('benchmark-card');
    if (card) {
      card.addEventListener('click', (e) => e.stopPropagation());
    }
    if (dismiss) {
      dismiss.addEventListener('click', onDismiss);
    }
  });
}

Important: Place the INDUSTRY_FLOOR and INDUSTRY_CEIL constants at module level (not inside the function). Keep all existing code (revealStats, animateCounter, animateGauge, GAUGE_CIRCUMFERENCE, celebrate stub) intact — only replace the showBenchmarkPopup export.

--- CHANGE 2: report/main.js ---

a) Add overlay.js import to the import block at the top of main.js. The current imports are:
  import { decodeReport } from '../shared/url-codec.js';
  import { getDistrict } from '../shared/districts.js';
  import { createMap, addVoterLayers } from './map.js';
  import { generateVoterData } from './data.js';
  import { playCameraSequence, runDotCascade } from './sequencer.js';

Add as the last import line:
  import { revealStats, showBenchmarkPopup } from './overlay.js';

b) Inside the async IIFE, find the Stage 3 placeholder block:
  // Stage 3 placeholder (Phase 3: reveal stat counters + benchmark popup)
  // await overlay.revealStats(payload);
  // await overlay.showBenchmarkPopup(payload);
  console.log('[main.js] Phase 2 sequence complete — ready for Phase 3 stats');

Replace the entire block (including the console.log) with:
  // Stage 3: Stat counters roll up from 0 — all four in parallel, resolve when last finishes
  await revealStats(payload);

  // Stage 4: Industry benchmark popup — celebrates deliverability vs 85-92% industry average
  // Resolves when user dismisses (tap/click) or automatically after overlay click
  await showBenchmarkPopup(payload);

  console.log('[main.js] Phase 3 sequence complete');

Do NOT change any other code in main.js. The onProgress and onBatchPing stubs in runDotCascade call remain as-is.
  </action>
  <verify>
    <automated>grep -n 'await revealStats' /home/vpliam/campaign-hype/report/main.js && grep -n 'await showBenchmarkPopup' /home/vpliam/campaign-hype/report/main.js && grep -n 'INDUSTRY_FLOOR' /home/vpliam/campaign-hype/report/overlay.js && grep -n 'CRUSHED' /home/vpliam/campaign-hype/report/overlay.js && grep -n 'innerHTML' /home/vpliam/campaign-hype/report/overlay.js | wc -l && cd /home/vpliam/campaign-hype && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep 'await revealStats' report/main.js returns 1 match
    - grep 'await showBenchmarkPopup' report/main.js returns 1 match
    - grep "import.*revealStats.*showBenchmarkPopup.*overlay" report/main.js returns 1 match
    - grep 'INDUSTRY_FLOOR' report/overlay.js returns 1 match
    - grep 'CRUSHED' report/overlay.js returns 1 match (the 92%+ message)
    - grep 'innerHTML' report/overlay.js returns 0 matches
    - grep 'Phase 2 sequence complete' report/main.js returns 0 matches (placeholder removed)
    - grep 'Phase 3 sequence complete' report/main.js returns 1 match (replaced)
    - npm run build exits 0
  </acceptance_criteria>
  <done>showBenchmarkPopup() implemented in overlay.js with correct celebration messages. report/main.js wires the complete sequence: flyToDistrict -> runDotCascade -> revealStats -> showBenchmarkPopup. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- npm run build exits 0
- The full sequence chain exists in main.js: playCameraSequence -> runDotCascade -> revealStats -> showBenchmarkPopup
- grep 'await revealStats' report/main.js returns 1 match
- grep 'await showBenchmarkPopup' report/main.js returns 1 match
- grep 'CRUSHED' report/overlay.js returns 1 match
- grep 'innerHTML' report/overlay.js returns 0 matches
- grep 'id="benchmark-popup"' report/index.html returns 1 match
- grep '@keyframes benchmark-slide-in' assets/report.css returns 1 match
</verification>

<success_criteria>
- showBenchmarkPopup() in overlay.js shows the popup with deliverabilityPct and contextual message (CRUSHED / Beat / neutral)
- Popup dismisses on button click or overlay background click, resolving its Promise
- main.js imports revealStats and showBenchmarkPopup from overlay.js
- main.js Stage 3 placeholder replaced: sequence now awaits revealStats then showBenchmarkPopup
- CELB-03 satisfied: complete map -> dots -> stats -> benchmark sequence with no overlap
- Vite build passes (npm run build exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/03-stats-and-sequence/03-02-SUMMARY.md`
</output>
