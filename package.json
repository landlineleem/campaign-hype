{
  "name": "campaign-hype",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "vite": "^8.0.1",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4",
    "maplibre-gl": "^5.21.0"
  }
}
